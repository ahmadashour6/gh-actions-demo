name: cron workflow

on:
  schedule:
    - cron: "*/5 * * * *"
    - cron: "*/20 1 * * *"

jobs:
  cron_workflow:
    name: Run Cron Task
    runs-on: ubuntu-latest
    steps:
      - run: echo "cron workflow"

